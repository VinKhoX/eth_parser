"
Implement eth_parser that does the following:
Parse 802.1Q (TPID 0x8100); accept VID 0 and 1 only; buffer payload, replay after EOP to fifo_wen[0]/[1] and raw_payload.
valid_pkt_cnt = accepted frames; invalid_pkt_cnt = non-VLAN, truncated, or rx_err; VID>1 = silent drop (no counter increment).
Ports: clk, rst, rx_valid, rx_data[7:0], rx_err, fifo_wen[1:0], raw_payload[7:0], valid_pkt_cnt[15:0], invalid_pkt_cnt[15:0].
Files: sources/eth_parser.sv, sources/payload_extract.sv; 
Must pass the Cocotb tests in tests/test_eth_parser.py.
"
